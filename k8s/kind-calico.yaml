kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  disableDefaultCNI: true
  podSubnet: "192.168.0.0/16" # matches Calico's default pool
nodes:
- role: control-plane
- role: worker

# Source:
# Based on previous personal configuration: https://github.com/Team-2-Devs/infra-core/blob/main/k8s/kind-calico.yaml